<h2>playlists#show</h2>
<h1>All Your Playlists</h1>

<div class="playlist">
  <h4>mood: <%= @playlist.mood %></h4>
  <h3 class='playlist-title' data-user-id="<%= current_user.id %>" data-playlist-id="<%= @playlist.id %>"><%= @playlist.title %><h3>

<div class="playlist" data-user-id="<%= current_user.id %>" data-playlist-id="<%= @playlist.id %>">

  <div class="playlist-songs"></div>

    <ul>
      <% @songs.each do |song| %>
      <li>
        <%= song.title %>
        <form action="/songs/<%= song.id %>" method='post'>
          <%= token_tag %>
          <input type="hidden" name='_method' value='delete'>
          <button type='submit'>delete song</button>
        </li>
        <% end %>
      </ul>

      <div>
        <a href="/users/<%= current_user.id %>/playlists">
          see your playlists
        </a>
      </div>

      <div>
        <a href="/users/<%= current_user.id %>/playlists/new">
          make a new playlist
        </a>
      </div>

      <%= link_to 'Edit Playlist', edit_user_playlist_path(current_user, @playlist.id) %>

      <form action="/users/<%= current_user.id %>/playlists/<%= @playlist.id %>" method='post'>
        <%= token_tag %>
        <input type="hidden" name='_method' value='delete'>
        <button type='submit'>delete playlist</button>
      </form>

    </div>
    <div class="viz">VISUALIZATION</div>

<!-- <ul class="sortable">
  <li class="ui-state-default"><span class="ui-icon ui-icon-arrowthick-2-n-s"></span>This is a test 1</li>
  <li class="ui-state-default"><span class="ui-icon ui-icon-arrowthick-2-n-s"></span>This is a test 2</li>
  <li class="ui-state-default"><span class="ui-icon ui-icon-arrowthick-2-n-s"></span>This is a test 3</li>
</ul> -->
